# Squid Forward Proxy — Ralph Worker
# Domain-level allowlist via CONNECT method (SSL bump 불필요)
# CONNECT 요청의 호스트명으로 dstdomain ACL 매칭

# --- ACL: Allowed Domains ---
acl allowed_domains dstdomain .github.com
acl allowed_domains dstdomain .githubusercontent.com
acl allowed_domains dstdomain registry.npmjs.org
acl allowed_domains dstdomain api.anthropic.com
acl allowed_domains dstdomain statsig.anthropic.com
acl allowed_domains dstdomain generativelanguage.googleapis.com
acl allowed_domains dstdomain statsig.com
acl allowed_domains dstdomain sentry.io

# --- ACL: Ports ---
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl CONNECT method CONNECT

# --- Access Rules ---
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow allowed_domains
http_access deny all

# --- Port ---
http_port 3128

# --- Logging ---
access_log stdio:/var/log/squid/access.log
cache_log /var/log/squid/cache.log

# --- Performance ---
cache deny all
dns_nameservers 127.0.0.11
forwarded_for delete
via off
